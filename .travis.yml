language: cpp
compiler:
  - gcc
  - clang
before_install:
  - sudo apt-add-repository ppa:linuxjedi/ppa
  - sudo apt-get update -qq
  - sudo apt-get install -y libuv-dev
script:
  - ./bootstrap
  - make check

notifications:
  irc: chat.freenode.net#libAttachSQL
